@model IEnumerable<Hyvinvointisovellus.Hymynaama>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Hymynaama1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tyontekijat.Sukunimi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Hymynaama1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tyontekijat.Sukunimi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HymynaamaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HymynaamaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HymynaamaID })
        </td>
    </tr>
}

</table>
